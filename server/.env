# Server
PORT=3001
NODE_ENV=development

# Database (MySQL via socat proxy on 3307)
DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=location_chat
DB_USER=root
DB_PASSWORD=qclaw_root_pass_2026

# Redis (via socat proxy on 6380)
REDIS_HOST=127.0.0.1
REDIS_PORT=6380

# JWT (must be at least 32 characters)
JWT_SECRET=location-chat-super-secret-jwt-key-2026-minimum-32-chars
